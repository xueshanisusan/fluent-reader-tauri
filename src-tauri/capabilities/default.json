{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the spike — allow webview defaults only.",
  "windows": ["main"],
  "permissions": ["core:default", "store:default"]
}
