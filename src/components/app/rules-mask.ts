// Filter-type-mask bit layout. Mirrors `src-tauri/src/rules.rs` constants —
// keep in sync (no codegen at this scale).
export const MASK_TITLE = 1
export const MASK_SNIPPET = 2
export const MASK_CREATOR = 4
